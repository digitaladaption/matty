# Project instructions

<!-- Template: install.sh copies this into a project if it has no CLAUDE.md yet. Edit per project. -->

## About this project
- What it is:
- Stack:
- How to run it:
- How to test it:

## Working style
- Make the smallest change that solves the task.
- Run the tests before saying something works.
- Ask before deleting files or touching anything outside this repo.

## Harness
- `/handoff` writes `HANDOFF.md`; the next session loads it automatically.
- Use the `reviewer` subagent before committing non-trivial changes.
